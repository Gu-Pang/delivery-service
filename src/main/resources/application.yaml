spring:
  application:
    name: delivery-server
  profiles:
    active: dev

  datasource:
    url: jdbc:postgresql://localhost:5432/gu_pang
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

server:
  port: 19020